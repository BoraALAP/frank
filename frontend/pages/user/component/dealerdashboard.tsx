import styled from "../styled-components";

const dealerdashboard = () => {
  return <Container>dealerdashboard</Container>;
};

const Container = styled.div`
  display: grid;
`;

export default dealerdashboard;
