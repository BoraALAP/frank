import React from "react";
import styled from "styled-components";

const contactUs = () => {
  return <Container>contactUs</Container>;
};

const Container = styled.div`
  display: grid;
`;

export default contactUs;
