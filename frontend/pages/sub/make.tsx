import React from "react";
import styled from "styled-components";

const make = () => {
  return <Container>make</Container>;
};

const Container = styled.div`
  display: grid;
`;

export default make;
