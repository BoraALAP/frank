import React from "react";
import styled from "styled-components";

const designoptions = () => {
  return <Container>designOptions</Container>;
};

const Container = styled.div`
  display: grid;
`;

export default designoptions;
